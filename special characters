#include <iostream>
#include <cctype>
using namespace std;
int main() {
    string s = "siddu@chowdary!123";
    int n = s.length();
    for (int i = 0; i < n; i++) {
        char c = s[i];
        if (!isalpha(c) && !isdigit(c)) {
            cout << c << " "; 
        }
    }
    return 0;  
}
