#include <iostream>
#include <cctype>
using namespace std;
int main() {
    string s = "sidduchowdary";
    int n = s.length();
    for (int i = 0; i < n; i++) {
        char c = s[i];
        if (c=='a'||c=='e'||c=='i'||c=='o'||c=='u') {
            continue;
        }
        else{
            cout<<c;
        }
    }
    return 0;  
}
